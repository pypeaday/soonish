# Temporal Configuration
TEMPORAL_URL=ghost:7233
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=soonish-task-queue

# Database Configuration
DATABASE_URL=sqlite+aiosqlite:///soonish.db

# Security Configuration (REQUIRED)
SECRET_KEY='''python -c "from cryptography.fernet import Fernet; print('ENCRYPTION_KEY=' + Fernet.generate_key().decode())'''
ENCRYPTION_KEY='''python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32)) '''

# Application Configuration
# DEBUG=false
# LOG_LEVEL=INFO

# Rate Limiting
# RATE_LIMIT_REQUESTS=100
# RATE_LIMIT_WINDOW=3600

# Dev SMS via SMTP
SMTP_APP_USER="app_user@gmail.com"
SMTP_APP_PASSWORD="app_password"
